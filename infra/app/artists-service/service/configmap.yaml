apiVersion: v1
kind: ConfigMap
metadata:
  name: artists-service-config
  namespace: music-catalog
data:
  # kv format
  key: "value"
  $actual-config-file$: "name.properties"

  # file format
  name.properties: |
    field=value1,value2
