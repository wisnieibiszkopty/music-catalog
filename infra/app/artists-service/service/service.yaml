apiVersion: v1
kind: Service
metadata:
  name: artist-service
  namespace: music-catalog
  labels:
    app.kubernetes.io/monitored-by: prometheus
spec:
  selector:
    app: artist-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  