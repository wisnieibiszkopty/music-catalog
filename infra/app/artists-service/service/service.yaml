apiVersion: v1
kind: Service
metadata:
  name: artists-service
  namespace: music-catalog
spec:
  selector:
    app: artists-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  