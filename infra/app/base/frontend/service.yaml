apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: music-catalog
  labels:
    app.kubernetes.io/monitored-by: prometheus
spec:
  type: ClusterIP
  selector:
    app: web-app
  ports:
    - port: 80
      targetPort: 80