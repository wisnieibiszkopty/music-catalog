apiVersion: v1
kind: Service
metadata:
  name: scraper-service
  namespace: music-catalog
  labels:
    app.kubernetes.io/monitored-by: prometheus
spec:
  type: ClusterIP
  selector:
    app: scraper-service
  ports:
    - port: 8080
      targetPort: 8080
