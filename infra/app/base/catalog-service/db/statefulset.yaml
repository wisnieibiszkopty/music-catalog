apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: catalog-db
  namespace: music-catalog
spec:
  serviceName: catalog-db
  replicas: 1
  selector:
    matchLabels:
      app: catalog-db
  template:
    metadata:
      labels:
        app: catalog-db
    spec:
      containers:
        - name: catalog-db
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: database
          resources:
            limits:
              cpu: "500m"
              memory: "100Mi"
            requests:
              cpu: "100m"
              memory: "50Mi"
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(CATALOG_DB_USER)
                - -d
                - $(CATALOG_DB)
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(CATALOG_DB_USER)
                - -d
                - $(CATALOG_DB)
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 5
          volumeMounts:
            - name: catalog-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: catalog-storage
          persistentVolumeClaim:
            claimName: catalog-data