apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: music-catalog
  labels:
    app.kubernetes.io/monitored-by: prometheus
spec:
  selector:
    app: api-gateway
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080