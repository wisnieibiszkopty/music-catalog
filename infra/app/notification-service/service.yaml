apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: music-catalog
spec:
  type: ClusterIP
  selector:
    app: notification-service
  sessionAffinity: ClientIP
  ports:
    - port: 8080
      targetPort: 8080